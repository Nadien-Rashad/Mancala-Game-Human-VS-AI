def Add_pit(value):
    """
    add the value by one when it’s called 
    Returns the value to be stored in the pocket or the store
    """
    temp_value = value
    bin = temp_value+1
    return bin
